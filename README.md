# reedshelf-website

Public ReedShelf marketing website.

## Run locally

```bash
npm install
npm run dev -- --port 5173
```

## Build

```bash
npm run build
```

The public site links to the separate web app using `VITE_WEBAPP_URL` (defaults to `http://localhost:5174`).
