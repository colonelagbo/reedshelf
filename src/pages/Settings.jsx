import { useState } from 'react';
import { Moon, Save } from 'lucide-react';
import { AppShell } from '../components/AppShell';
import { getCurrentUser, getSettings, saveSettings } from '../lib/appStore';
export function Settings(){const user=getCurrentUser();const [dark,setDark]=useState(getSettings(user.id).darkMode);const save=()=>{saveSettings(user.id,{darkMode:dark});document.documentElement.classList.toggle('dark',dark)};return <AppShell><div className="mx-auto max-w-3xl"><h1 className="text-3xl font-bold">Settings</h1><p className="mt-1 text-[#7b8c84]">Manage your reading experience.</p><div className="mt-7 rounded-3xl border border-[#dfe5dc] bg-white p-6 sm:p-8"><div className="flex items-center justify-between gap-4 border-b border-[#edf0eb] pb-6"><div className="flex gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#eff5df]"><Moon size={19}/></span><div><p className="font-semibold">Dark mode</p><p className="mt-1 text-sm text-[#7b8c84]">Prepare the app for a darker reading environment.</p></div></div><button type="button" onClick={()=>setDark(v=>!v)} className={`relative h-7 w-12 rounded-full transition ${dark?'bg-[#6d853d]':'bg-[#cbd5c6]'}`} aria-pressed={dark}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white transition ${dark?'left-6':'left-1'}`}/></button></div><button onClick={save} className="mt-6 inline-flex items-center gap-2 rounded-xl bg-[#18332b] px-5 py-3 text-sm font-semibold text-white"><Save size={17}/> Save settings</button></div></div></AppShell>}
